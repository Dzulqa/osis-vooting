server {
    listen 80;
    server_name suara.smkalamanah.sch.id;

    # Optional: redirect HTTP to HTTPS (uncomment when you have SSL configured)
    # return 301 https://$host$request_uri;

    location / {
        # Forward requests to PM2 Next.js server running on port 3008
        proxy_pass http://127.0.0.1:3008;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Real IP headers
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Optional: HTTPS Configuration (Uncomment and configure path to SSL certs)
# server {
#     listen 443 ssl http2;
#     server_name suara.smkalamanah.sch.id;
# 
#     ssl_certificate /etc/letsencrypt/live/suara.smkalamanah.sch.id/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/suara.smkalamanah.sch.id/privkey.pem;
# 
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
